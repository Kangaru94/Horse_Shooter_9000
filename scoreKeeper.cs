using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This entire script is just to contain the player score
// There is probably a better way to do it, but after many hours, it finally works
public class scoreKeeper : MonoBehaviour
{
    // This variable is referenced only in the DetectCollusions script
   public static int score = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
